import type { IndexRecord } from '../types.js';
export class DynamicIndexer { private observer: MutationObserver | null = null; constructor(private extract: (root: Document | Element) => IndexRecord[], private onRecords: (records: IndexRecord[]) => void) {} start(root: Document | Element = document): void { if (typeof MutationObserver === 'undefined') return; let timer: ReturnType<typeof setTimeout> | undefined; this.observer = new MutationObserver(() => { clearTimeout(timer); timer = setTimeout(() => this.onRecords(this.extract(root)), 100); }); this.observer.observe(root, { childList:true, subtree:true, characterData:true }); } stop(): void { this.observer?.disconnect(); this.observer = null; } }
