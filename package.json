{
  "name": "spotlight-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/spotlight.ts --bundle --format=iife --global-name=SpotlightSearch --outfile=dist/spotlight.min.js --minify",
    "test": "node --import tsx --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "esbuild-wasm": "^0.28.1",
    "tsx": "^4.20.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
